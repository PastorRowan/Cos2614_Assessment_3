
#pragma once

#include "models/VehicleRepository.h"

namespace helpers {

    /**
     * populateVehicleRepository
     *
     * Utility functions for inserting sample or initial vehicle data into a models::VehicleRepository
     */
    void populateVehicleRepository(models::VehicleRepository* vehicleRepository);

};
